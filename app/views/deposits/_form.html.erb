<%= form_with(model: deposit) do |form| %>
  <% if deposit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deposit.errors.count, "error") %> prohibited this deposit from being saved:</h2>

      <ul>
        <% deposit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-md-12">
      <div class="form-group">
        <%= form.label :sender %>
        <%= form.select :sender_id, options_for_select(Stock.all.map{|c| [c.code, c.id]}, form.object.sender_id), {}, class: "form-control" %>
      </div>
    </div>

    <div class="col-md-12">
      <div class="form-group">
        <%= form.label :receiver %>
        <%= form.select :receiver_id, options_for_select(Account.all.map{|c| [c.number, c.id]}, form.object.receiver_id), {}, class: "form-control" %>
      </div>
    </div>

    <div class="col-md-12">
      <div class="form-group">
        <%= form.label :amount %>
        <%= form.text_field :amount, class: "form-control" %>
      </div>
    </div>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
